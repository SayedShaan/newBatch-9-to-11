<!doctype html>
<html>
<head><meta charset="utf-8"><title>Simple Function</title></head>
<body>
  <h3>Simple user-defined function</h3>

  <label>Number: <input id="num" value="5" type="number"></label>
  <button onclick="doubleFromUI()">Double it</button>

  <p id="out"></p>

  <script>
    // Named function (user-defined)
    function double(x) {
      return Number(x) * 2;
    }

    // Helper called by the button
    function doubleFromUI() {
      const val = document.getElementById('num').value;
      const result = double(val);
      document.getElementById('out').textContent = 'Result: ' + result;
      console.log('double(', val, ') ->', result);
    }

    // You can also call the function from the console:
    // double(10)  -> 20
  </script>
</body>
</html>
